import styled from 'styled-components'

export const ResumeCardContainer = styled.div`
  > strong {
    font-size: 1.15rem;
    font-weight: 600;
    margin-bottom: 0.5rem;
  }

  .MuiPaper-root {
    border-radius: 25px;
  }

  > div > :first-of-type {
    padding: 1rem;
  }
`
