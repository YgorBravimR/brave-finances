import styled from "@emotion/styled";

export const ResumeCardContainer = styled.div`
  > p {
    font-size: 1.15rem;
    font-weight: 500;
    margin-bottom: 0.5rem;
  }

  .MuiPaper-root {
    border-radius: 25px;
  }

  > div > :first-of-type {
    padding: 1rem;
  }
`;
